//第五章习题13
/***************************************************
- 用迭代法求 x=sqrt(a)。求平方根的迭代公式为
- xn+1 = 1/2 * (xn + a/xn)
- 要求前后两次求出的x的差的绝对值小于1e-5。
***************************************************/

#include <stdio.h>
#include <math.h>
int main()
{
	double a, x1=1.0, x2=1.0;
	printf("请输入一个数字：");
	scanf("%lf",&a);
	do	
	{	
		x1 = x2;	
		x2 = 1.0/2 * (x1 + a/x1);				
	}while(fabs(x2-x1) >= 1e-5);
	printf("sqrt(%lf) = %lf",a,x2);
	return 0;
} 
