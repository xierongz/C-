//例4.5
/*********************************************
- 有一函数 ：
-  	  { -1  (x < 0)
- y = { 0   (x = 0)
-     { 1   (x > 0)
- 编一程序，输入一个x值，要求输出相应的y值。
*********************************************/

#include <stdio.h>

int main()
{
	float x;
	int y;
	printf("请输入x的值：\n");
	scanf("%f",&x);
	if(x < 0)
		y = -1;
	else
		y = (x==0) ? 0 : 1;
	
	printf("y的值：%d\n",y);
}
  
